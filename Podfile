# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'HemnetAssignment' do
  use_frameworks!

end

target 'HemnetAssignmentTests' do
  inherit! :search_paths
  use_frameworks!
  
  # Pods for testing
  pod 'Nimble', :modular_headers => true
  pod 'Nimble-Snapshots', :modular_headers => true
  pod 'Quick'
end


